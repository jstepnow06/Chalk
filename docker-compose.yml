services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      DATABASE_PATH: /data/chalk.db
    volumes:
      - chalk-data:/data
    restart: unless-stopped

volumes:
  chalk-data:
